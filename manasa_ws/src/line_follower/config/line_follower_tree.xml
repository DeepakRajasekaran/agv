<root BTCPP_format="4">
    <BehaviorTree ID="MainTree">
        <Sequence>
            <!-- Check if /navigation/start was triggered -->
            <Fallback>
                <Sequence>
                    <IsNavigationStarted is_started="{is_started}"/>
                    <SetLineFollowerState enable="true"/>
                    <PublishNavState state_str="FOLLOW_LINE"/>
                </Sequence>
                <Sequence>
                    <SetLineFollowerState enable="false"/>
                    <PublishNavState state_str="IDLE"/>
                </Sequence>
            </Fallback>
        </Sequence>
    </BehaviorTree>
</root>
